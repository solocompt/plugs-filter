============
Installation
============

At the command line::

    $ easy_install plugs-filter

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv plugs-filter
    $ pip install plugs-filter
